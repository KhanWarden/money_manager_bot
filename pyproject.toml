[project]
name = "money-manager"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytz>=2026.2",
]

[tool.uv.workspace]
members = [
    "bot",
]

[dependency-groups]
dev = [
    "black>=26.3.1",
]
