[project]
name = "bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "KhanWarden", email = "baryrhan2004@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.27.0",
    "pydantic-settings>=2.14.0",
]

[project.scripts]
bot = "bot:main"

[build-system]
requires = ["uv_build>=0.11.11,<0.12.0"]
build-backend = "uv_build"
